TODO

cat << EOF | << EOF > t.txt

env -i ./minishell incrementer la variable $SHLVL

./minishell ./minishell
ensuite ctrl +c

export TEST="-l                  -a"
ls $TEST
    -> voir ls
ls "$TEST"
    -> ne PAS voir ls

revoir expension variable (ordre)
    -> environement
    -> ~
    -> wildcard
    -> commandes

 echo hi >

cat <<EOF | grep "important" | wc -l > nombre.txt

cat <<<test #modifie parsing ajouter erreur si 3 < a la suite

cat << < test ou cat < << test ou cat < > test... plusieurs redirection

cat <<oef | grep "oui" | wc -l >nbr <<oef